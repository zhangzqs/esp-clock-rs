export enum PageRouteTable {
    home,
    menu,
}

export global PageRouter {
    out property <PageRouteTable> current-page;
    public function goto-page(p: PageRouteTable) {
        current-page = p;
    }
}